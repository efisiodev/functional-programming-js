// EJEMPLO TO REVERSED

const numbers = [1, 2, 3, 4, 5];

  // Sin usar toReversed
  // numbers.reverse();
  // console.log(numbers); // Resultado [ 5, 4, 3, 2, 1 ]

  // Usando toReversed
  const reversedNumbers = numbers.toReversed();
  console.log(reversedNumbers); // Resultado [ 5, 4, 3, 2, 1 ]